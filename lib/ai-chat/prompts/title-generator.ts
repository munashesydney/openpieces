export const TITLE_GENERATOR_PROMPT = `Generate a concise title (2–5 words) for a conversation based on the user's first message.

Rules:
- Respond with ONLY the title — no quotes, no prefixes, no punctuation, no explanation
- Keep it 2–5 words
- Capture the core topic or intent of the message
- Do not add labels like "Title:" or "Conversation about"`;
